name: country_cli
version: 1.0.0
environment:
  sdk: '^3.8.1'
resolution: workspace
dependencies:
  http: ^1.2.0
  logging: ^1.2.0
  terminal_colors:
    path: ../terminal_colors
  country_api:
    path: ../country_api
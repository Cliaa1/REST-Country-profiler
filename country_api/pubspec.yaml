name: country_api
publish_to: 'none'
version: 1.0.0
environment:
  sdk: '^3.8.1'
resolution: workspace
dependencies:
  http: ^1.2.0
  logging: ^1.2.0
  terminal_colors:
    path: ../terminal_colors
dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0